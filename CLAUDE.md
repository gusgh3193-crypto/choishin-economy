# 최신경제 — 프로젝트 가이드

경제/재테크 정보 블로그 "최신경제"를 운영하는 부업 사이트 프로젝트다.
목표는 매일 글 발행과 사이트 점검을 통해 검색에 잘 노출되는 사이트를 만드는 것이다.

## 조직 구조

- **사장**: 사용자 본인. 모든 의사결정의 최종 승인권자.
- **마스터** (`.claude/agents/master.md`): 사장의 지시를 받아 아래 5개 부서에 업무를 배분하고, 산출물을 검수하고, 진행 상황을 통제한다. 사장이 특정 부서를 지목하지 않고 작업을 요청하면 마스터가 먼저 응대한다.
- **부서 (5개)**:
  | 부서 | 담당 파일 | 역할 |
  |---|---|---|
  | 기획팀 | `.claude/agents/planning-team.md` | 사이트 방향, 콘텐츠 주제/캘린더, 키워드 전략 |
  | 개발팀 | `.claude/agents/dev-team.md` | 사이트 제작 및 수리, 기술적 이슈 해결 |
  | 마케팅팀 | `.claude/agents/marketing-team.md` | 홍보, SNS 카드뉴스 제작 |
  | CS팀 | `.claude/agents/cs-team.md` | 방문자·고객 문의 응대 |
  | 디자인팀 | `.claude/agents/design-team.md` | 화면 UI/UX 다듬기 |
- **블로그 글 작성 담당** (`.claude/agents/blog-writer.md`): 기획팀이 정한 방향에 따라 실제 글을 쓰는 전문 인력. 기획팀 또는 마스터가 호출한다.

## 콘텐츠 규칙

- 글은 `posts/YYYY-MM-DD-슬러그.md` 형식으로 저장한다.
- 모든 글은 SEO를 위해 title, meta_description, keywords 프런트매터를 갖춘다.
- 투자·대출 등 금전적 판단에 영향을 줄 수 있는 내용에는 면책 문구를 넣는다.

## 배포

- 아직 실제 배포 플랫폼(워드프레스/티스토리/정적 사이트 등)이 정해지지 않았다. 정해지기 전까지는 `posts/`에 마크다운으로만 축적한다.

## 업무 보고 및 검증 규칙 (필수, 예외 없음)

사장이 직접 결과물을 열어 확인하지 않아도 되도록, 모든 직원(마스터 포함)은 아래 규칙을 지킨다.

1. **"완료했다"고 보고하기 전에 반드시 직접 확인한다.**
   - 파일을 만들거나 고쳤다면 Read로 다시 읽어서 실제로 그 내용이 저장됐는지 확인한다.
   - 스크립트/명령을 만들었다면 실제로 실행해서 출력 결과를 확인한다 (실행 안 해보고 "작동할 것"이라고 쓰지 않는다).
   - git commit/push를 했다면 `git log`, `git status`, 그리고 원격 저장소 기준(`git ls-remote` 또는 `git log origin/main`)으로 실제로 반영됐는지 확인한다. 로컬 커밋만 되고 push가 실패했으면 "완료"가 아니라 "push 실패"로 보고한다.
2. **보고에는 확인한 증거를 함께 남긴다.** "~했습니다"가 아니라 파일 경로, 실제 내용 일부, 명령어 실행 결과, 커밋 해시처럼 확인 가능한 근거를 포함한다.
3. **확인하지 못한 것은 "완료"라고 쓰지 않는다.** 직접 열어보거나 실행해보지 못했다면 "미검증" 또는 "확인 못 함"이라고 명시한다. 실패했거나 오류가 났으면 성공한 것처럼 포장하지 않고 오류 내용을 그대로 보고한다.
4. **마스터는 부서의 보고를 그대로 믿지 않는다.** 각 부서가 "완료했다"고 보고해도, 마스터는 실제 결과물(파일, 커밋, 실행 결과)을 자신이 다시 열어보거나 실행해서 재검증한 뒤에만 `daily-logs/`에 "완료"로 기록한다. 재검증에서 문제가 발견되면 해당 부서에 보완을 지시하고, 로그에는 문제와 재지시 내용을 함께 남긴다.
5. 이 규칙은 마스터, 기획팀, 개발팀, 마케팅팀, CS팀, 디자인팀, 블로그 글 작성 담당 모두에게 동일하게 적용된다.
